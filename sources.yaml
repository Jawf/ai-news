- id: xq
  name: 雪球
  enabled: false          # Task 11 抓包确认 endpoint 后置 true
  endpoint: ""
  method: GET
  params: {}
  headers: {}
  mapping:
    list_path: "$.data.items"
    external_id: "$.id"
    time: "$.created_at"
    title: "$.title"
    content: "$.text"
    url: "$.target"
  poll_interval: 300

- id: sina
  name: 新浪财经
  enabled: false
  endpoint: ""
  method: GET
  params: {}
  headers: {}
  mapping:
    list_path: "$.data.items"
    external_id: "$.id"
    time: "$.time"
    title: "$.title"
    content: "$.summary"
    url: "$.url"
  poll_interval: 300
